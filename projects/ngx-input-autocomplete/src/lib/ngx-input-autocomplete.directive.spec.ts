import { NgxInputAutocompleteDirective } from './ngx-input-autocomplete.directive';

describe('NgxInputAutocompleteDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxInputAutocompleteDirective();
    expect(directive).toBeTruthy();
  });
});
