import { NgxTextAutocompleteDirective } from './ngx-text-autocomplete.directive';

describe('NgxTextAutocompleteDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxTextAutocompleteDirective();
    expect(directive).toBeTruthy();
  });
});
